[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "fabricator"
version = "0.0.0"
requires-python = ">=3.9"

[project.scripts]
fabricator = "tools.cli:cli"

[tool.setuptools]
packages = ["tools"]